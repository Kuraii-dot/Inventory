# Smart Inventory documentation

- `INVENTORY_SUPABASE_CLOUD_SETUP.md` describes the current cloud deployment.
- `legacy/` contains the retired LAN/Tailscale setup and historical material notes for reference only.

TCMS/mobile-specific SQL remains in the OS project because it modifies the TCMS Inspector Supabase project rather than the Inventory database.
